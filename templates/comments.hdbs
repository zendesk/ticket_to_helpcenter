<ul class="comment_list">
  <h5>Comments: </h5>
  {{#each comments}}
    {{#if public}}
      <li class="comment" id="{{id}}"><span class="time">{{created_at_local}}</span> <span class="comment">{{{html_body}}}</span></li>
    {{/if}}
  {{/each}}
</ul>